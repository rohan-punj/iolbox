# iolab runtime resource allocation — SINGLE SOURCE OF TRUTH.
# Change these two numbers, then rebuild (or re-`pct set`/edit) the target to apply.
#   IOLAB_VCPUS  = vCPUs the runtime guest/container is allocated
#   IOLAB_RAM_MB = RAM in megabytes
IOLAB_VCPUS=4
IOLAB_RAM_MB=4096
