# iolbox runtime resource allocation — SINGLE SOURCE OF TRUTH.
# Change these two numbers, then rebuild (or re-`pct set`/edit) the target to apply.
#   IOLBOX_VCPUS  = vCPUs the runtime guest/container is allocated
#   IOLBOX_RAM_MB = RAM in megabytes
IOLBOX_VCPUS=4
IOLBOX_RAM_MB=4096
