# docker/.env — mirrors runtime/resources.env (the single source of truth
# for vCPU/RAM across every iolab deployment target). Used by
# docker/compose.yml's cpus:/mem_limit: substitution.
IOLAB_VCPUS=4
IOLAB_RAM_MB=4096
